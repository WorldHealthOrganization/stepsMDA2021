ROUTEOUT 'C:\STEPS\EpiInfo\Output Tables\TPdangerWT.htm' REPLACE
READ 'C:\STEPS\EpiInfo\STEPS.mdb':data1
RELATE data2 qr :: qr
WRITE REPLACE "Epi 2000" 'C:\STEPS\EpiInfo\STEPS.mdb':temp AgeRange Sex Valid TP1a TP1b TP1c PSU Stratum WStep1

READ 'C:\STEPS\EpiInfo\STEPS.mdb':temp

DEFINE C

RECODE TP1a to C
1 = "1) noticed information in newspapers"
2 = "2) did not notice information in newspapers"
END

WRITE REPLACE "Epi 2000" 'C:\STEPS\EpiInfo\STEPS.mdb':temp * 
READ 'C:\STEPS\EpiInfo\STEPS.mdb':temp

DEFINE D

RECODE TP1b to D
1 = "1) noticed information on TV"
2 = "2) did not notice information on TV"
END

WRITE REPLACE "Epi 2000" 'C:\STEPS\EpiInfo\STEPS.mdb':temp * 
READ 'C:\STEPS\EpiInfo\STEPS.mdb':temp

DEFINE E

RECODE TP1c to E
1 = "1) noticed information on radio"
2 = "2) did not notice information on radio"
END

WRITE REPLACE "Epi 2000" 'C:\STEPS\EpiInfo\STEPS.mdb':temp * 
READ 'C:\STEPS\EpiInfo\STEPS.mdb':temp

DEFINE F

IF  (TP1b=1 OR TP1c=1) THEN
F = "1) noticed information on TV or radio"
END

IF (TP1b=2 AND TP1c=2) THEN
F="2) did not notice information on TV or radio"
END

WRITE REPLACE "Epi 2000" 'C:\STEPS\EpiInfo\STEPS.mdb':temp * 
READ 'C:\STEPS\EpiInfo\STEPS.mdb':temp

DEFINE TP1aCLN
DEFINE TP1bCLN
DEFINE TP1cCLN

IF (Valid=1) THEN
TP1aCLN=1
TP1bCLN=1
TP1cCLN=1
ELSE
TP1aCLN=2
TP1bCLN=2
TP1cCLN=2
END

WRITE REPLACE "Epi 2000" 'C:\STEPS\EpiInfo\STEPS.mdb':temp * 
READ 'C:\STEPS\EpiInfo\STEPS.mdb':temp

IF (C=(.)) THEN
TP1aCLN=2
END

IF (D=(.)) THEN
TP1bCLN=2
END

IF (E=(.)) THEN
TP1cCLN=2
END

WRITE REPLACE "Epi 2000" 'C:\STEPS\EpiInfo\STEPS.mdb':temp * 
READ 'C:\STEPS\EpiInfo\STEPS.mdb':temp
 
HEADER 2 "Noticing information on TV, newspapers/magazines or radio about dangers of smoking"
HEADER 4 "Program: TPdangerWT"

SELECT TP1aCLN=1
SELECT Sex="Men"
HEADER 5 "Men"
TABLES AgeRange C STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT

SELECT TP1aCLN=1
SELECT Sex="Women"
HEADER 5 "Women"
TABLES AgeRange C STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT

SELECT TP1aCLN=1
HEADER 5 "Both Sexes"
TABLES AgeRange C STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT

SELECT TP1bCLN=1
SELECT Sex="Men"
HEADER 5 "Men"
TABLES AgeRange D STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT

SELECT TP1bCLN=1
SELECT Sex="Women"
HEADER 5 "Women"
TABLES AgeRange D STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT

SELECT TP1bCLN=1
HEADER 5 "Both Sexes"
TABLES AgeRange D STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT

SELECT TP1cCLN=1
SELECT Sex="Men"
HEADER 5 "Men"
TABLES AgeRange E STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT

SELECT TP1cCLN=1
SELECT Sex="Women"
HEADER 5 "Women"
TABLES AgeRange E STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT

SELECT TP1cCLN=1
HEADER 5 "Both Sexes"
TABLES AgeRange E STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT

SELECT TP1bCLN=1
SELECT TP1cCLN=1
SELECT Sex="Men"
HEADER 5 "Men"
TABLES AgeRange F STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT

SELECT TP1bCLN=1
SELECT TP1cCLN=1
SELECT Sex="Women"
HEADER 5 "Women"
TABLES AgeRange F STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT

SELECT TP1bCLN=1
SELECT TP1cCLN=1
HEADER 5 "Both Sexes"
TABLES AgeRange F STRATAVAR=Stratum WEIGHTVAR=Wstep1 PSUVAR=PSU
SELECT


HEADER 1 RESET
HEADER 2 RESET
HEADER 3 RESET
HEADER 4 RESET
HEADER 5 RESET
