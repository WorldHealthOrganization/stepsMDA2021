library(here)
if(.Platform$OS.type == "unix") {
# Mac
library(Hmisc) # needed to load mdb data in MacOS
data1 <- mdb.get("UKR2019STEPSAnon.mdb", tables = "data1")
data2 <- mdb.get("UKR2019STEPSAnon.mdb", tables = "data2")
} else {
# Windows
library(RODBC)
channel <- odbcConnectAccess("UKR2019STEPSAnon.mdb")
data1<-sqlFetch(channel,"data1", as.is=T)
data2<-sqlFetch(channel,"data2", as.is=T)
odbcClose(channel)
}
